name: Update Data

on:
  schedule:
    - cron: "*/5 * * * *"  # Runs every 5 minute

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    - name: Run update script
      run: python update_data.py  # Modify this based on your script location
